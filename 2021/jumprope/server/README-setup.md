Run this server on the Google instance.
Ensure the port is reachable (or configure `docker-compose.yml`)
Provide the IP address and port of the server in the public description.

docker-compose up -d --build

